name: Windows build (test)

# Manual trigger + on every push, so you can just click "Run workflow"
# from the Actions tab, or push a commit to see it run automatically.
on:
  workflow_dispatch:
  push:
    branches: [ master, main ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # Installs a prebuilt Qt6 kit for MinGW, plus the matching MinGW
      # compiler toolchain itself, via aqtinstall.
      # NOTE: exact version/arch strings drift over time -- if this step
      # fails, check https://ddalcino.github.io/aqt-list-server/ for
      # currently-valid combinations and adjust 'version' / 'arch' / 'tools'.
      - name: Install Qt6 (MinGW)
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          arch: 'win64_mingw'
          modules: 'qtmultimedia'
          tools: 'tools_mingw1310'
          set-env: true

      - name: Install Ninja
        uses: seanmiddleditch/gha-setup-ninja@master

      # The project's CMakeLists.txt requires static libstdc++.a / libgcc.a /
      # libgcc_eh.a. Rather than trust `g++ -print-file-name` (which returned
      # a mangled/incomplete result last run and needs investigating), do a
      # real recursive filesystem search across the actual Qt MinGW install
      # so we know for certain what's actually shipped and where.
      - name: Locate static MinGW runtime libraries
        shell: pwsh
        run: |
          $mingwRoot = (Split-Path (Split-Path (Get-Command g++).Source))
          Write-Host "MinGW root: $mingwRoot"

          Write-Host "--- Full directory tree (first 200 entries) ---"
          Get-ChildItem -Recurse -Path $mingwRoot -ErrorAction SilentlyContinue |
            Select-Object -First 200 -ExpandProperty FullName

          Write-Host "--- Searching for libstdc++*.a, libgcc*.a anywhere under MinGW root ---"
          $found = Get-ChildItem -Recurse -Path $mingwRoot -ErrorAction SilentlyContinue -Include "libstdc++*.a","libgcc*.a"
          $found | ForEach-Object { Write-Host $_.FullName }

          $stdcxx = $found | Where-Object { $_.Name -eq "libstdc++.a" } | Select-Object -First 1
          $gcc    = $found | Where-Object { $_.Name -eq "libgcc.a" }    | Select-Object -First 1
          $gccEh  = $found | Where-Object { $_.Name -eq "libgcc_eh.a" } | Select-Object -First 1

          if (-not $stdcxx -or -not $gcc -or -not $gccEh) {
            Write-Warning "One or more static runtime archives were not found anywhere under $mingwRoot."
            Write-Warning "This likely means this MinGW distribution only ships the shared runtime (DLLs), not static archives."
            # Don't fail here -- let the facts above be visible in the log so we can decide the next step.
          } else {
            echo "PC6001VX_STATIC_STDCXX=$($stdcxx.FullName)" >> $env:GITHUB_ENV
            echo "PC6001VX_STATIC_GCC=$($gcc.FullName)" >> $env:GITHUB_ENV
            echo "PC6001VX_STATIC_GCC_EH=$($gccEh.FullName)" >> $env:GITHUB_ENV
          }

      # GitHub's windows-latest runners ship a bare MSYS2 base install --
      # the actual mingw-w64 toolchain packages (which provide the Win32
      # import libraries like libadvapi32.a) aren't installed by default.
      # Install just the small headers/crt package that provides them via
      # pacman, rather than switching compilers or doing anything more
      # invasive. This only supplies stub/import libraries (symbol tables
      # for OS DLLs), not compiled code, so there's no ABI concern mixing
      # it with Qt's own MinGW compiler.
      - name: Install mingw-w64 import libraries (pacman)
        shell: cmd
        run: |
          C:\msys64\usr\bin\bash.exe -lc "pacman -Sy --noconfirm mingw-w64-x86_64-headers mingw-w64-x86_64-crt mingw-w64-x86_64-SDL2 mingw-w64-x86_64-libvorbis mingw-w64-x86_64-gettext"

      - name: Locate Win32 import libraries (MSYS2)
        shell: pwsh
        run: |
          $names = @{
            "libadvapi32.a"    = "ADVAPI32"
            "libSDL2.a"        = "SDL2"
            "libogg.a"         = "OGG"
            "libvorbis.a"      = "VORBIS"
            "libvorbisenc.a"   = "VORBISENC"
            "libvorbisfile.a"  = "VORBISFILE"
            "libintl.a"        = "INTL"
          }
          $dirs = @()
          $missing = @()

          foreach ($name in $names.Keys) {
            $hit = Get-ChildItem -Recurse -Path 'C:\msys64' -ErrorAction SilentlyContinue -Filter $name | Select-Object -First 1
            if (-not $hit) {
              # Fall back to the .dll.a import-library variant -- since we
              # use an exact absolute path (not -lNAME search), this still
              # links fine, it just means the final .exe depends on the
              # matching runtime DLL at runtime instead of being fully static.
              $dllaName = $name -replace '\.a$', '.dll.a'
              $hit = Get-ChildItem -Recurse -Path 'C:\msys64' -ErrorAction SilentlyContinue -Filter $dllaName | Select-Object -First 1
              if ($hit) {
                Write-Warning "$name not found as a true static archive; using dynamic import lib $dllaName instead. The built .exe will need the matching runtime DLL alongside it."
              }
            }
            if ($hit) {
              Write-Host "FOUND: $name -> $($hit.FullName)"
              $dirs += $hit.DirectoryName
              $varName = "PC6001VX_LIBPATH_$($names[$name])"
              echo "$varName=$($hit.FullName)" >> $env:GITHUB_ENV
            } else {
              Write-Warning "MISSING: $name (and $($name -replace '\.a$', '.dll.a'))"
              $missing += $name
            }
          }

          if ($missing.Count -gt 0) {
            Write-Error "Could not locate: $($missing -join ', '). Check the pacman install step above."
            exit 1
          }

          # Still needed for CMAKE_LIBRARY_PATH: SingleApplication's own
          # find_library(advapi32) call at configure time resolves to an
          # absolute path via this variable, which is safe (configure-time
          # only, not a link-time -L that could shadow CRT libraries).
          $dirs = $dirs | Select-Object -Unique
          echo "PC6001VX_EXTRA_LIBRARY_PATH=$($dirs -join ';')" >> $env:GITHUB_ENV


      - name: Configure (CMake)
        run: >
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DNOAVI=ON
          -DNOSOUND=ON
          "-DCMAKE_CXX_FLAGS=-DNOLIBINTL"
          "-DPC6001VX_STATIC_STDCXX=${env:PC6001VX_STATIC_STDCXX}"
          "-DPC6001VX_STATIC_GCC=${env:PC6001VX_STATIC_GCC}"
          "-DPC6001VX_STATIC_GCC_EH=${env:PC6001VX_STATIC_GCC_EH}"
          "-DCMAKE_LIBRARY_PATH=${env:PC6001VX_EXTRA_LIBRARY_PATH}"
          "-DPC6001VX_LIB_SDL2=${env:PC6001VX_LIBPATH_SDL2}"
          "-DPC6001VX_LIB_OGG=${env:PC6001VX_LIBPATH_OGG}"
          "-DPC6001VX_LIB_VORBIS=${env:PC6001VX_LIBPATH_VORBIS}"
          "-DPC6001VX_LIB_VORBISENC=${env:PC6001VX_LIBPATH_VORBISENC}"
          "-DPC6001VX_LIB_VORBISFILE=${env:PC6001VX_LIBPATH_VORBISFILE}"
          "-DPC6001VX_LIB_INTL=${env:PC6001VX_LIBPATH_INTL}"
        shell: pwsh

      - name: Build
        run: ninja -C build PC6001VX

      - name: Bundle Qt DLLs (windeployqt)
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Recurse -Filter "PC6001VX.exe" -Path build | Select-Object -First 1
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item $exe.FullName -Destination dist\
          windeployqt --qmldir . dist\PC6001VX.exe

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: PC6001VX-windows-test-build
          path: dist\
